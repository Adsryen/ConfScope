# 数据服务 - 开发
app:
  name: data-svc

storage:
  # 开发本地盘
  type: local
  path: /var/data/dev
  # 开发不限大小
  quota-gb:
  # 备份: 开发不配
  backup:
    # 全部留空
    cron:
    target:

# 数据保留
retention:
  # 开发保留 7 天
  days: 7
  # 归档: 开发不归档
  archive:
    enabled: false
    # 目标留空
    destination:
